PORT=3000
EVENT_LOG_PATH=./data/events.jsonl

# Pi can use provider credentials already configured in ~/.pi/agent/auth.json.
# Alternatively, expose one supported provider key to the process:
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
